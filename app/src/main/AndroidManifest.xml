<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.nth.test.animations">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sceneAnimations.SceneAnimationActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailsActivity" />
        <activity
            android:name=".activitytransitions.AnimatedListView"
            android:theme="@style/blackNWhite" />
        <activity
            android:name=".activitytransitions.AnimatedDetailsActivity"
            android:theme="@style/blackNWhite" />
        <activity
            android:name=".activitytransitions.NonAnimatedListView"
            android:theme="@style/blackNWhite" />
        <activity android:name=".SimpleSamplesActivity" />
        <activity android:name=".layouttransitions.TransitionListActivity"></activity>
    </application>

</manifest>